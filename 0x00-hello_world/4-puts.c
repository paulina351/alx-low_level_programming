#include <stdio.h>
/**
 *main - The entry point of our program where the runtime will start executing.
 *Return: 0 (success)
 */

int main(void)
{
	puts("Programming is like building a multilingual puzzle);
	return (0);
}
