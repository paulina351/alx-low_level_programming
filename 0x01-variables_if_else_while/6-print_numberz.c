#include <stdio.h>
/**
 * main - Entry point of the program
 *
 * Return: 0 Always (success)
 */
int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
